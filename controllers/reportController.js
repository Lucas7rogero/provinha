const Student = require('../models/student');
const Teacher = require('../models/teacher');
const Database = require('../models/database');
// cria o const database

async function showReport(req, res) {}

module.exports = {
  showReport
};